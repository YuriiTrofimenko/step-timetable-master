export default class DayOfWeekModel {
  public dayNumber: number
  public dayName: string
  constructor(dayNumber: number, dayName: string) {
    this.dayNumber = dayNumber
    this.dayName = dayName
  }
}